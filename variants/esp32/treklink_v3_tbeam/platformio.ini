[env:treklink-v3-tbeam]
extends = env:tbeam
build_flags =
  -I variants/esp32/treklink_v3_tbeam
  ${env:tbeam.build_flags}
  -D TREKLINK_VARIANT
  -D TREKLINK_V3
  -D TREKLINK_ONBOARD_QUEUE=1               # specs/onboard-queue; bounds are the TrekLinkQueueConfig.h defaults (1 MiB LittleFS)
